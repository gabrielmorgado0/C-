#include <iostream>
#include <cmath>

int main()
{
    double x = 3.99;
    double y = 4;
    double z;
    
    //z = std::max(x, y); mostra o maior
    //z = std::min(x, y); mostra o menor
    //z = pow(2, 4); elevar a potencia
    //z = sqrt(100); raiz quadrada
    //z = abs(-100); oque falta para chegar a zero
    //z = round(x); arredonda o numero dependendo do q tiver, se tiver mais perto do 3.15=3 ou do 4.5+=4, pelo q entendi foi isso
    //z = ceil(x); aredonda um numero pra cima do q tiver
    //z = floor(x); arredonda pra baixo, mas pelo q entendi seria assim: 3.99 = 3
    std::cout << z;


    return 0;
}